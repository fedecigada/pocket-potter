# MongoDB
# Local: mongodb://localhost:27017
# Atlas: use the non-SRV connection string if DNS resolution is slow
DB_URI=
DB_NAME=

# Harry Potter API
HP_API_URL=https://hp-api.onrender.com

# JWT
JWT_SECRET=
JWT_EXPIRES_IN=7d

# Backend port
PORT=3100

# Origin allowed by CORS
FRONTEND_URL=http://localhost:5173
